# Kept for pnpm 9 and older, which read node-linker from here. pnpm 10+ ignores
# this key and reads `nodeLinker` from pnpm-workspace.yaml instead — see that
# file for why a flat node_modules is required. Both are present because this
# repo pins no pnpm version.
node-linker=hoisted
